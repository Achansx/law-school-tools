---
id: "09"
title: "Cost and Labor: The Honest Accounting"
status: ready_for_stitch
target_words: 700
word_count: 761
last_phase: polish
draft_status: needs_polish
cite_status: needs_polish
polish_status: ready_for_stitch
footnotes_count: 16
---

# IX. Cost and Labor: The Honest Accounting

## A. The accounting the article promised

The workplan names a cost-and-labor table as one of the five most-important gaps before this article can ship; the existing record’s single anchored figure (about sixty percent of weekly Claude usage to reach the 198-page progress snapshot) does not split by phase, by labor category, or by dollar count.[^1] [^2] The workplan also fixes the column set: hours by category (ingest, prompt tuning, review, debugging), token spend by phase, hosting cost, total dollars, and total person-hours.[^3] Section IX inherits the column set without inventing a fifth category and without dropping one. The sixty-percent figure stays as the human-side starting anchor; the per-phase and per-category breakdown lands through a retroactive reconstruction against the email trail, the git log, and best-memory entries committed to Appendix-Cost-Log.md, with prospective per-tick logging from the snapshot date forward.[^4]

## B. Hosting cost: bounded under normal operation, blocked when credits exhaust

The deployed-site hosting bill is a two-state architectural property rather than a single number. The preferred deploy path is Netlify CLI direct upload from the scheduled task, which does not consume Netlify build minutes because the build runs in the local sandbox and only the produced assets cross the wire; the Netlify MCP fallback is demoted because it does consume build minutes.[^5] Under normal operation, the deploy phase lands at zero marginal hosting cost on the free tier. The honest hedge is the credit-block pattern: when account credits exhaust, the free-tier ceiling reasserts itself as an HTTP 403 carrying the body “Account credit usage exceeded,” and three sequential credit-block events on May 7, 2026, at 18:41Z, 19:11Z, and 19:42Z tripped the dispatcher’s three-strike guard, which skips the fast-deploy POST until human action restores credits or upgrades the tier.[^6] [^7]

## C. Wall-clock and cadence anchors

Three vault-primary wall-clock figures and two cadence figures anchor the per-tick line. The fast-deploy script completes in approximately six seconds and fits inside the forty-five-second sandbox cap; the clean CLI-path deploy on the 205-page corpus logs approximately sixteen seconds build plus approximately eight seconds upload; the Step 3.5 verification budget is approximately ten to fifteen seconds across the three sub-checks (page sample, search sanity probe, source-materials sample).[^8] The full deploy-phase wall-clock floor reads as approximately twenty to forty seconds end-to-end depending on which upload path the run takes. The vault scheduled task fires every thirty minutes with an Ingest budget of three to five files per run, and the article scheduled task fires every two hours, observable in the BUILD_NARRATIVE entry timestamps.[^9] The vault Ingest cap is ten files per tick across new files and Step 8 backfills (fifteen when Steps 1 through 7 produce nothing), enforced by a *scope_discipline* score of zero (run score 4.5 instead of 5.0) when crossed.[^10]

## D. Per-tick token-economy: tiered loading and per-phase rubric

The controlling per-tick token-economy mechanism is tiered context loading. Every scheduled-task run loads PROJECT_PRIMER, the state file, and the runbook section for the current phase as Tier 1, and the current phase’s rubric, LESSONS, the current month’s BUILD_NARRATIVE, and PERSONAS for Verify only as Tier 2; other rubrics and archived narratives are not loaded.[^11] The same architectural property runs in the vault scheduled task per its own runbook and tiered-loading lesson. The article’s own L-007 names the cross-rubric-reading prohibition explicitly because cross-rubric loading would create conflicting incentives at the rubric level and conflicting token-spend ceilings at the cost level. The load policy makes one tick of one phase a predictable token unit, the precondition for the cadence-times-per-tick-spend back-computation the master cost-table row depends on.[^12]

## E. The master table, the downstream consumers, and what is still open

The four labor categories the workplan names map cleanly to the system’s own phases.[^13] Ingest maps to the vault Ingest phase and the Article Harvest phase. Prompt tuning maps to the per-tick rubric-and-LESSONS work the author does offline, plus the Article Outline and Cite work that builds each phase’s prompt commitments. Review maps to the per-tick scorecard reads, the periodic LESSONS Consolidate sweep, and the Verify-phase forced-finding adversarial pass. Debugging maps to the per-tick pending-issue resolution work and the multi-tick pending-issue lifecycle the system carries across rotations. The table reads as instrumentation of the loop rather than a separate accounting exercise. Three downstream consumers forward-reference this owning table: PI-003 for Section IV’s ingest line, PI-018 for Section VIII’s deploy line, and PI-042 for Section VII’s iteration line.[^14] Three Section IX pending issues frame what is still outstanding: PI-049 names the master cost-and-labor table reconstruction against the workplan’s retroactive backfill protocol,[^15] PI-050 names the external pricing lookups deferred to Cite for Anthropic API pricing, Netlify free-tier and upgrade pricing, and any metered MCP service prices, and PI-051 names the person-hours backfill across the four labor categories with honest confidence ranges, the methodology in a footnote so a reader can replicate it against the same vault and git history.[^16]

## Footnotes

[^1]: *See infra* App. A (Vault Architecture and File Layout) (Article-Workplan.md §3.1 enumerating the cost-and-labor table as one of the five most-important gaps the article must close before it can ship; same section identifying the existing record’s single anchored figure as a hand-wave that does not split by phase, by labor category, or by snapshot-anchored dollar count; Article-Workplan.md §4 Phase A item 2 naming the retroactive-reconstruction protocol as email trail plus git log plus best-memory entries committed to `Appendix-Cost-Log.md`); *see also infra* App. C (Cost Log), Methodology Note (workplan §8 retroactive-reconstruction protocol with the four-labor-category mapping in note [^13] *infra*).

[^2]: *See infra* App. A (Vault Architecture and File Layout) (progress report to Professor Chandler at the 198-page snapshot of 92 case briefs, 27 doctrinal topic pages, and 79 lecture summaries, naming approximately sixty percent of weekly Claude usage consumed to reach that snapshot, with verbatim excerpt at App. D (Correspondence Excerpts)); *see also supra* Section IV (The Input Corpus) note 33 (cross-reference to the same progress-report source used as Section IV’s 198-page snapshot anchor per L-011 snapshot discipline and L-014 App.-A-with-App.-D routing). The anchored figure is the author’s self-reported subscription utilization at the snapshot date and does not split by phase, labor category, or dollar count, which is exactly the gap the workplan §3.1 cited *supra* at note [^1] requires this section to close.

[^3]: *See supra* note [^1] (workplan §3.1 column-set enumeration: hours by category (ingest, prompt tuning, review, debugging), token spend by phase, hosting cost, total dollars, total person-hours; same section forbidding invention of a fifth aggregate column and prohibiting dropping a named column).

[^4]: *See infra* App. C (Cost Log), tbl. 9.1 (master cost-and-labor table per the workplan §3.1 column-set, assembled per the workplan §4 Phase A item 2 retroactive-reconstruction protocol; capture pending under PI-049, with PI-003 Section IV ingest line, PI-018 Section VIII deploy line, and PI-042 Section VII iteration line as downstream consumers per L-027 numeric-line-item-routing and L-032 single-owner-section convention; in-section row references use the L-033 local Table 9.N numbering rather than the downstream-consumer *See infra* form).

[^5]: *See infra* App. A (Vault Architecture and File Layout) (DEPLOY.md Step 2 designating the Netlify CLI direct-upload path as canonical specifically because it does not consume Netlify build minutes and uses the local PAT so deploys are reproducible from the scheduled task without proxy-token TTL pressure; DEPLOY.md fallback-path entry demoting the Netlify MCP `netlify-deploy-services-updater` call with `operation: deploy-site` and `siteId: f78a098b-9a9e-412a-8d4f-dd8ccda13bfe` because it consumes Netlify build minutes); *see also supra* Section VIII (From Vault to Website) note 78 (cross-reference to the same DEPLOY.md Step 2 cited in Section VIII’s build-pipeline framing of the deploy path).

[^6]: *See infra* App. A (Vault Architecture and File Layout) (vault `LESSONS.md` Deployment item 5 naming the HTTP 403 “Account credit usage exceeded” pattern as an external block rather than a code defect, recording that build still succeeds and upload aborts before any deploy record is created, and specifying the RUNBOOK guard fires after `consecutive_same_type_failures>=3` and `type=deploy-credit-blocked` to skip the fast-deploy POST until credits are restored; same entry’s Seen-record logging 2026-05-07T18:41Z, T19:11Z, and T19:42Z as the three sequential credit-block events that tripped the three-strike guard against a 281-page corpus build of 131 cases, 53 topics, and 97 lectures).

[^7]: *See infra* App. C (Cost Log), tbl. 9.2 (deploy-phase line per PI-018; two-state hosting framing carrying the qualitative free-tier-bounded-under-normal-operation property paired with the credit-block-when-credits-exhaust hedge, without fabricating a dollar figure for either state; in-section row reference uses L-033 local numbering per the convention named at note [^4] *supra*).

[^8]: *See infra* App. A (Vault Architecture and File Layout) (vault `LESSONS.md` Deployment item 1 recording that `.site/fast_deploy.py` walks `dist/`, POSTs the manifest, uploads only required bytes, and calls finalize, completing in approximately six seconds and fitting inside the forty-five-second sandbox cap; vault `CHANGELOG.md` 2026-05-01 deploy entry logging a clean CLI-path deploy at approximately sixteen seconds build plus approximately eight seconds upload on a 205-page corpus build of 100 cases, 28 topics, and 77 lectures via `netlify-cli --no-build --prod --dir .` from `.site/dist`; DEPLOY.md Step 3.5 verification budget at approximately ten to fifteen seconds total across the three sub-checks of page sample, search sanity probe, and source-materials sample, capturing the wall-clock cost in the run summary before Step 4 advances `last_deploy`). The aggregated approximately twenty-to-forty-second deploy-phase wall-clock floor is the sum of the build, upload, and verification ranges and is presented as a range rather than a point estimate per L-001 numeric-caveat discipline applied to anchored-empirical-precision claims.

[^9]: *See infra* App. A (Vault Architecture and File Layout) (vault `CHANGELOG.md` line 198 documenting the vault scheduled-task cadence as a thirty-minute interval with tiered context loading and an Ingest budget of three to five files per run; Article `BUILD_NARRATIVE_2026-05.md` entry timestamps for 2026-05-19 demonstrating the two-hour article scheduled-task cadence at Cite 05:00:00Z, Polish 07:00:00Z, Stitch 11:00:00Z, and Verify 13:00:00Z, with the same two-hour spacing observable across all entries in the rolling narrative). The two cadences are the multipliers that, combined with the per-tick token-economy mechanism at note [^11] *infra*, let the master cost-table row at note [^4] *supra* back-compute weekly token spend against the [^2] sixty-percent-of-weekly-Claude anchor.

[^10]: *See infra* App. A (Vault Architecture and File Layout) (vault `LESSONS.md` Ingest combined-cap entry specifying the cap at ten across new files plus Step 8 backfills per tick, with the backfill budget equal to `max(0, 10 - new_files_count)` and the exception that when Steps 1 through 7 produce zero new files the backfill-only ceiling rises to fifteen; same entry naming the enforcement mechanism as a `scope_discipline 0` score that drops the run score from 5.0 to 4.5 when the cap is crossed; Seen-record dated 2026-04-22T18:45Z capturing the eight-new-plus-three-backfill total of eleven that triggered the red flag).

[^11]: *See infra* App. A (Vault Architecture and File Layout) (Article `RUNBOOK.md` Dispatcher protocol enumerating the per-run loads as Tier 1 covering `Article/.article-state.json`, `Article/rubric/<current_phase>.md`, and `Article/LESSONS.md`, with Tier 2 covering the current phase’s rubric, LESSONS, the current month’s `BUILD_NARRATIVE_YYYY-MM.md`, and `PERSONAS.md` only for Verify, and naming the explicit prohibition “Do not read other rubrics. Do not read archived narratives.”; Article `LESSONS.md` L-007 (per-phase rubric only) recording that each run loads only its own phase rubric because cross-rubric reading creates conflicting incentives, with the same dispatcher-protocol architecture mirrored in the vault scheduled task per its own `RUNBOOK.md` and the vault `LESSONS.md` tiered-loading rule); *see also supra* Section VI (Prompting as Pedagogical Design) note 63 (cross-reference to the prompt-design framing of the same tiered-loading architecture as the `LESSONS.md`-as-prompt-iteration-memory contract).

[^12]: *See infra* App. C (Cost Log), tbl. 9.3 (per-tick token-spend by phase carrying the cadence-times-per-tick-spend back-computation against the [^2] sixty-percent-of-weekly-Claude anchor with the cadence multipliers from note [^9] *supra* and the per-tick token-economy mechanism from note [^11] *supra*; capture pending under PI-049 with the L-001 numeric-caveat discipline requiring verified figures only).

[^13]: *See supra* note [^1] (workplan §3.1 enumerating ingest, prompt tuning, review, and debugging as the four labor-category columns the cost-and-labor table must carry). The body-prose mapping from the four labor categories to the system’s own phases (ingest to vault Ingest plus Article Harvest; prompt tuning to per-tick rubric-and-LESSONS work plus Article Outline and Cite; review to per-tick scorecard reads plus the periodic LESSONS Consolidate sweep plus the Verify-phase forced-finding adversarial pass; debugging to per-tick pending-issue resolution plus the multi-tick pending-issue lifecycle) is this article’s contribution rather than the workplan’s; the workplan names the columns and this section maps them to the loop architecture Sections III through VIII develop.

[^14]: *See infra* App. A (Vault Architecture and File Layout) (Article `.article-state.json` pending_issues registry entries: PI-003 opened 2026-05-15T20:05:00Z naming the ingest-phase token spend and Claude-API cost attributable to Section IV’s input-corpus work; PI-018 opened 2026-05-17 naming the deploy-phase cost line per the third-rotation Harvest tick BUILD_NARRATIVE; PI-042 opened 2026-05-18 naming the iteration-phase cost line per the sixth-rotation Harvest tick BUILD_NARRATIVE), with all three downstream consumers forward-referencing this owning section per L-027 numeric-line-item-routing rule and L-032 single-owner-section convention; *see also supra* Section IV (The Input Corpus) note 22 (Section IV ingest-line forward reference closing through this section’s master table); *see also supra* Section VII (Iterative Improvement Under Professorial Control) note 68 (Section VII iteration-line forward reference closing through this section’s master table); *see also supra* Section VIII (From Vault to Website) note 82 (Section VIII deploy-line forward reference closing through this section’s master table).

[^15]: *See infra* App. C (Cost Log), tbl. 9.4 (master cost-and-labor table assembled per the workplan §8 retroactive-reconstruction protocol with PI-003 Section IV ingest line, PI-018 Section VIII deploy line, and PI-042 Section VII iteration line closing through this table once it lands; PI-049 captures the master-table-reconstruction gap with the L-032 single-owner-section convention enumerating the three downstream consumers in PI-049’s note block).

[^16]: *See infra* App. C (Cost Log), Methodology Note (git-log walk across `Chandler Constitutional Law Vault/.git` plus best-memory reconstruction across the four labor categories of ingest, prompt tuning, review, and debugging, presenting per-category hour ranges with honest confidence intervals rather than point figures per L-001 numeric-caveat discipline applied to anchored-empirical-precision claims about author labor; PI-051 captures the person-hours-backfill-protocol gap and closes when the four per-category hour ranges land in the App. C Methodology Note with the methodology footnoted at the row-level so a reader can replicate the backfill against the same vault and the same git history).
