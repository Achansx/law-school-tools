---
section: "10"
fact_type: argument
source_path: "Chandler Constitutional Law Vault/Article/RUNBOOK.md"
verified: true
notes: "Grounds X.C's claim that the production process, not just the schema, generalizes: the six-phase rotation, the lessons file that carries memory between runs, the adversarial review pass required to return a finding, and the professorial checkpoints are method features rather than constitutional-law features. The RUNBOOK is the source-of-truth artifact for these mechanisms and is present in the repo (dispatcher protocol, the seven-phase rotation, the Verify three-persona pass that must return a finding, the gate stop-conditions). The verified-source-discipline half of X.C carries one substitution: a case-law course checks quotations against the indexed opinion; a statute-heavy course checks against the code and its history. That substitution's TOOLING is hand-waved and is logged as PI-089; this card supplies the verified loop facts and marks the statute substitution as an open gap rather than a settled transfer."
---

Section X.C claims the loop and its discipline transfer alongside the schema, and the RUNBOOK is the verified source for the loop facts. The method runs a fixed phase rotation (Harvest, Outline, Draft, Cite, Polish, Stitch, Verify), carries memory between runs in a capped lessons file, requires an adversarial review pass in which each of three personas must return at least one finding, and gates advancement on human checkpoints that can hold any phase (see RUNBOOK dispatcher protocol, the Verify three-persona procedure, and the gate stop-conditions). None of these mechanisms is constitutional-law-specific; they are features of the method. The verified-source discipline transfers with one substitution: a case-law course checks each quotation against the indexed judicial opinion, while a statute-heavy course would check against the code and its history instead. That substitution is asserted at the level of discipline but not of tooling; the case study's verification depends on a case-law-specific indexed-source service, and what a statutory adaptation would use is not specified. PI-089 logs that tooling gap; this card grounds the loop-and-discipline transfer claim and flags the statute substitution as unresolved.

Cross-reference: `RUNBOOK.md` (dispatcher protocol, Verify procedure, stop-conditions) for the phase rotation, lessons-memory, forced-finding review, and professorial gating; PI-089 (open) for the statute-verification tooling gap behind X.C sentence 2.
