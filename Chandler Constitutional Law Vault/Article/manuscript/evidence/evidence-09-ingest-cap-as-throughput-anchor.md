---
section: "09"
fact_type: statistic
source_path: "Chandler Constitutional Law Vault/LESSONS.md"
verified: true
notes: "The bounded throughput anchor that lets cost-per-page-ingested be back-computed for Section IX's ingest-phase line and that closes PI-003 (Section IV ingest-phase cost line) without fabricating a token figure. Vault LESSONS.md Ingest entry sets the combined cap at 10 across new files plus Step 8 backfills per tick (15 when Steps 1 through 7 produce zero new files), enforced by `scope_discipline 0` (run score 4.5 instead of 5.0) when the cap is crossed. The vault scheduled task fires every 30 minutes per evidence-09-scheduled-task-cadence, so the upper bound on ingest throughput is 10 files per 30-minute tick or 480 files per day before the per-page Ingest rubric scoring penalizes overrun. Section IX's ingest-phase line uses this as the labor-and-token anchor: the bounded throughput establishes a per-page floor for Ingest's wall-clock and token spend, and the rubric-enforced cap is what makes the per-tick cost predictable. The card's claim is the cap itself (verified vault primary source), not a fabricated dollar or token figure per page (still open under PI-049); the floor lets the cost-table line carry a 'roughly N pages per tick at bounded token spend' qualitative framing while the numeric per-page figure stays open."
---

The vault Ingest cap is ten across new files plus Step 8 backfills per tick, enforced by a `scope_discipline 0` score (dropping run score from 5.0 to 4.5) when the cap is crossed. The exception is fifteen when Steps 1 through 7 produce zero new files. With the vault scheduled task firing every thirty minutes per evidence-09-scheduled-task-cadence, the upper bound on ingest throughput is ten files per tick or roughly four hundred eighty files per day before the rubric-enforced ceiling penalizes overrun. Section IX's ingest-phase cost line uses this as the labor-and-token anchor: the bounded throughput establishes a per-page floor for Ingest's wall-clock and token spend, and the rubric-enforced cap is what makes the per-tick cost predictable. The cap is the vault-primary fact this card carries (verified); the per-page token-spend figure that would let the table assert dollars per Ingest tick stays open under PI-049 until the workplan §8 backfill protocol runs.

Exact source quote, `Chandler Constitutional Law Vault/LESSONS.md` Ingest cap (line 111):

> **Ingest combined cap = 10 across new files + Step 8 backfills, even in burst mode.** Backfill budget = `max(0, 10 - new_files_count)`. Exception: when Steps 1–7 produce zero new files, the backfill-only ceiling rises to 15. **Why:** crossing the cap triggers `scope_discipline 0` (run score 4.5 instead of 5.0). _Seen: 2026-04-22T18:45Z (8 new + 3 backfill = 11 → red flag)._
