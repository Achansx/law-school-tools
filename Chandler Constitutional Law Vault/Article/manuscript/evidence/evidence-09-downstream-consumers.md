---
section: "09"
fact_type: argument
source_path: "Chandler Constitutional Law Vault/Article/.article-state.json"
verified: true
notes: "The downstream-consumer registry for the master cost table. Three already-open pending issues route through Section IX per the L-027 numeric-line-item-routing rule and the L-032 shared-asset owner-section convention: PI-003 (Section IV ingest-phase cost line; opened 2026-05-15), PI-018 (Section VIII deploy-phase cost line; opened 2026-05-17), PI-042 (Section VII iteration-phase cost line; opened 2026-05-18). Each downstream consumer carries the qualitative architectural property in its own prose and forward-references Section IX for the numeric line. Per L-032 the master PI Section IX opens (PI-049 in the next-tick state write) must enumerate all three downstream consumers in its note block so the dependency chain is traceable in one place, and per L-033 the in-section table rows use local 'Table 9.N' or 'cost-table row N' numbering rather than the downstream-consumer *See infra* form. Section IX's draft does not invent ingest, deploy, or iteration figures and does not propagate any number Sections IV, VII, or VIII carry as a placeholder; the table is the canonical source and the consuming sections forward-cite it once the table lands. The card scope is the consumer registry itself; the table rows land during Draft once the workplan §8 backfill reconstruction completes."
---

Three pending issues already open against Sections IV, VII, and VIII forward-reference Section IX's master cost-and-labor table per the L-027 numeric-line-item-routing rule and the L-032 shared-asset owner-section convention. PI-003 names the ingest-phase line for Section IV (opened 2026-05-15 during the first Harvest tick), PI-018 names the deploy-phase line for Section VIII (opened 2026-05-17 during the third-rotation Harvest tick), and PI-042 names the iteration-phase line for Section VII (opened 2026-05-18 during the sixth-rotation Harvest tick). Each downstream consumer carries the qualitative architectural property in its own prose — the ingest pipeline shape in IV, the deploy pipeline shape in VIII, the iteration loop shape in VII — and forward-references Section IX for the numeric line rather than fabricating a figure inline. The master PI Section IX opens this Harvest tick (PI-049) enumerates all three downstream consumers so the dependency chain is traceable in one place, and per L-033 the in-section table rows use local Table 9.N numbering rather than the downstream-consumer *See infra* form. Section IX's draft does not invent ingest, deploy, or iteration figures; the table is the canonical source and the consuming sections forward-cite it once the table lands.

Exact source quote, `Chandler Constitutional Law Vault/Article/.article-state.json` pending_issues PI-003 (Section IV ingest-phase cost):

> {"id": "PI-003", "section": "04", "opened_at": "2026-05-15T20:05:00Z", "phase": "harvest", "type": "evidence-gap", "claim": "Exact token spend and Claude-API cost attributable to ingest-phase work on the input corpus.", "status": "open", "note": "Workplan §3.1 calls for a real cost table. Section IX owns the full table; Section 04 needs at least the ingest-phase line. Backfill from memory plus git log per workplan §8. Outline 4.5 names this as the gap to close."}

PI-018 (Section VIII deploy-phase cost line) and PI-042 (Section VII iteration-phase cost line) carry analogous owner-section forward-reference language per the BUILD_NARRATIVE_2026-05.md entries for 2026-05-17 (Section 08 Harvest tick) and 2026-05-18 (Section 07 Harvest tick).
