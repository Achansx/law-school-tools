---
section: "05"
fact_type: example
source_path: "Chandler Constitutional Law Vault/PROJECT_PRIMER.md"
verified: true
notes: "The three-folder content taxonomy plus Templates, Source Materials, rubric, and archive. This is the load-bearing structural fact of Section V: the vault is not a flat note pile, it is a typed schema with three first-class artifact kinds. Each folder corresponds to a different unit of legal-pedagogy work (a decided case, a doctrine, a class session) and a different template."
---

The vault organizes itself into three primary content folders — `Cases/`, `Topics/`, and `Lectures/` — each backed by a canonical template in `Templates/` and a per-page YAML frontmatter schema. `Source Materials/` is read-only input; `rubric/` holds the per-phase scoring criteria; `archive/` rotates older narrative logs. The three content folders correspond to three distinct units of legal-pedagogy work: a decided case (with a 9-section brief), a doctrine (with a synthesis page), and a class session (with a lecture summary). This typing is what makes the vault behave as a schema rather than a folder of prose.

Exact source quote, `Chandler Constitutional Law Vault/PROJECT_PRIMER.md` lines 30 to 36 (Vault Structure section):

> - `Cases/` — one file per case, follows `Templates/Case Brief.md` (9-section case-briefer standard)
> - `Topics/` — doctrine/concept pages, follows `Templates/Topic Page.md`
> - `Lectures/` — lecture and class-recap summaries, follows `Templates/Lecture Summary.md`
> - `Templates/` — page templates (schema source of truth)
> - `Source Materials/` — read-only corpus (PDFs, PPTXs, `Uploaded Media/`)
> - `rubric/` — per-phase scoring rubrics (one file per phase)
> - `archive/` — rotated older files (narratives, legacy rubrics)
